#### HEADER -------

## This code creates firm-year-level measures of network exposure to the carbon shock

# 1. downstream exposure_it = \sum_j x_{ijt} (1+allowance shortage_{jt})
# 2. upstream exposure_it = \sum_j x_{jit} (1+allowance shortage_{jt})

#####################



